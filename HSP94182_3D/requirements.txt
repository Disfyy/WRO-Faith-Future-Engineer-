# HSP 94182 1/16 — CAD-модель (зависимости Python)
# ----------------------------------------------------------------
# Установка:
#     pip install -r requirements.txt
#
# Что устанавливается:
#   cadquery       — параметрическое CAD-моделирование на Python
#                    использует OpenCascade (OCCT) под капотом
#   cadquery-ocp   — OCCT биндинги (тянутся автоматически с cadquery)
#
# Размер на диске: ~500 МБ (в основном OpenCascade библиотеки).
# Поддерживаемые ОС: macOS, Linux, Windows. Python ≥ 3.9.
# ----------------------------------------------------------------

cadquery>=2.5.0
