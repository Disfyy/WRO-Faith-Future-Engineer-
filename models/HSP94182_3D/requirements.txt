# HSP 94182 1/16 -- CAD model (Python deps)
# ----------------------------------------------------------------
# Install:
#     pip install -r requirements.txt
#
# What it pulls in:
#   cadquery       -- parametric Python CAD,
#                     uses OpenCascade (OCCT) under the hood
#   cadquery-ocp   -- OCCT bindings (auto-installed with cadquery)
#
# On-disk size: ~500 MB (mostly the OpenCascade libraries).
# Supported OS: macOS, Linux, Windows. Requires Python >= 3.9.
# ----------------------------------------------------------------

cadquery>=2.5.0
